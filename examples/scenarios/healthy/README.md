# Healthy update

`./scripts/ota-demo up` runs this first.

Expected state: `committed`. The simulator reboots into the new slot, the health file is present, and the agent marks the slot good.

This is not a Minewing result.
