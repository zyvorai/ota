# Third-party notices

The vendored Go module `github.com/godbus/dbus/v5` v5.1.0 is distributed under its
upstream BSD-style license; see `vendor/github.com/godbus/dbus/v5/LICENSE`.

The Go standard library/toolchain is used to build the binaries and is covered
by the Go project license. The toolchain itself is not bundled in this source ZIP.

RAUC, systemd, polkit, the Linux kernel and board BSP are external device-image
dependencies. This repository does not include or relicense their binaries.
The device image distributor remains responsible for their license obligations.
